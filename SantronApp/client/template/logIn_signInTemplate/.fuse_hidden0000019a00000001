<template name="login_signInTemplate">
    <div class="loginPageCss">
        <div class="appName_loginPageCss">
            Santron Computers
        </div>
        <div class="col-lg-4 col-md-4 col-sm-10 col-xs-10 col-lg-offset-4 col-md-offset-4 col-sm-offset-1 col-xs-offset-1 loginBackgroundStyle">
            <div class="">
                {{#if loginTemplate}}
                    {{> Login}}
                {{/if}}

                {{#if registerTemplate}}
                    {{> Register}}
                {{/if}}

                {{#if forgotPwTemplate}}
                    {{> ForgotPassword}}
                {{/if}}
            </div>
        </div>
    </div>

    <!-- {{> ResetPassword}} -->
</template>